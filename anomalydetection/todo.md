# Sistemas de anomalias por Z-Score

features:
        -adicionar uma tabela no formato adequado
        -retirar as linhas com valores nulos
        -criar a função de division
        -criar a função que compara cada elemento com o subgrupo dele
        -se for acima de 3 desvios padrões, eu acrescento como anomalia 1
        -se for abaixo de 3 desvios padrões, eu acrescento como anomalia negativa -1
        -crio o dataframe de interpretação
        -

